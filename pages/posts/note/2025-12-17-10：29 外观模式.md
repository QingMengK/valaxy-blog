---
title:
  - 外观模式
created: 2025-12-17
updated: 2025-12-17
type: 📘 知识节点
tags:
  - 面向对象
  - 设计模式
---
> 摘要：外观模式通过引入一个统一的外观类，将复杂子系统的调用过程封装起来，使客户端以更简单的方式使用系统，从而降低系统耦合度，提高代码的可维护性。

## 一、模式背景（小故事）

想象这样一个场景：

你要**启动一台电脑**，真实情况下内部会发生很多事：

- 检查电源
    
- 初始化 CPU
    
- 加载内存
    
- 启动硬盘
    
- 启动操作系统
    

但作为用户，你只做了一件事：

👉 **按下电源键**

你**不关心**内部的复杂步骤，也**不希望**每次都手动调用这些子系统。

**外观模式**就是为了解决这个问题而出现的。

---

## 二、外观模式是什么？

> **外观模式（Facade Pattern）**  
> 为子系统中的一组复杂接口提供一个**统一、简单的入口**，  
> 使客户端可以更容易地使用子系统。

一句话总结：

👉 **把复杂的系统，包装成一个简单的“门面”**

---

## 三、不使用外观模式的情况

### ❌ 问题代码（客户端直接操作多个子系统）

```c++
class CPU {
    public void Start() {
        Console.WriteLine("CPU 启动");
    }
}

class Memory {
    public void Load() {
        Console.WriteLine("内存加载");
    }
}

class Disk {
    public void Read() {
        Console.WriteLine("硬盘读取");
    }
}

class Program {
    static void Main() {
        CPU cpu = new CPU();
        Memory memory = new Memory();
        Disk disk = new Disk();

        cpu.Start();
        memory.Load();
        disk.Read();
    }
}

```

### ❌ 存在的问题

- 客户端 **知道太多细节**
    
- 调用顺序容易出错
    
- 子系统一改，客户端全改
    
- 使用成本高，不利于维护
    

---

## 四、使用外观模式的情况（推荐）

### 1️⃣ 子系统（保持不变）

```c++
class CPU {
    public void Start() {
        Console.WriteLine("CPU 启动");
    }
}

class Memory {
    public void Load() {
        Console.WriteLine("内存加载");
    }
}

class Disk {
    public void Read() {
        Console.WriteLine("硬盘读取");
    }
}

```

---

### 2️⃣ 外观类（Facade）

```c++
class ComputerFacade {
    private CPU cpu = new CPU();
    private Memory memory = new Memory();
    private Disk disk = new Disk();

    public void StartComputer() {
        cpu.Start();
        memory.Load();
        disk.Read();
        Console.WriteLine("电脑启动完成");
    }
}
```

---

### 3️⃣ 客户端使用（极简）

```c++
class Program {
    static void Main() {
        ComputerFacade computer = new ComputerFacade();
        computer.StartComputer();
    }
}

```

---

## 五、外观模式的核心思想

| 角色         | 作用           |
| ---------- | ------------ |
| 子系统        | 负责具体功能       |
| 外观（Facade） | 封装子系统，提供统一入口 |
| 客户端        | 只与外观交互       |

👉 **客户端不再直接依赖子系统**

---

## 六、外观模式的优点

✅ **简化使用**  
✅ **降低耦合**  
✅ **提高可维护性**  
✅ **符合迪米特法则（最少知道原则）**

---

## 七、外观模式 vs 其他模式（容易混淆）

|模式|关注点|
|---|---|
|外观模式|简化接口|
|适配器模式|改变接口以兼容|
|装饰模式|增强功能|
|代理模式|控制访问|

👉 **外观模式不增强、不修改功能，只是“更好用”**