---
title:
created: 2025-12-14
updated: 2025-12-14
type: 📘 知识节点
tags:
  - 面向对象
  - 设计模式
---
> 摘要：桥接模式通过“组合”代替“继承”，将抽象和实现解耦，使系统在多维度变化时更加灵活、可扩展。
## 一、桥接模式是什么？

**桥接模式（Bridge Pattern的核心思想是：

> **将“抽象”和“实现”分离，使它们可以独立变化。**

一句更白话的话：

> **不要让“功能维度”和“实现维度”绑死在继承层次里，用组合把它们“桥接”起来。**

---

## 二、一个小故事（背景）

你在做一个**跨平台图形绘制系统**：

- 有不同的**图形**：
    
    - 圆形（Circle）
        
    - 矩形（Rectangle）
        
- 又要支持不同的**绘制方式 / 平台**：
    
    - Windows
        
    - Linux
        

### 问题来了 ❓

如果你直接用继承，会发生什么？

---

## 三、不使用桥接模式（继承爆炸问题）

### ❌ 直接继承的做法
```text
Shape
 ├── WindowsCircle
 ├── WindowsRectangle
 ├── LinuxCircle
 └── LinuxRectangle
```
### C++ 示例（❌ 不推荐）

```cpp
class WindowsCircle {
public:
    void draw() {
        cout << "Draw Circle on Windows" << endl;
    }
};

class LinuxCircle {
public:
    void draw() {
        cout << "Draw Circle on Linux" << endl;
    }
};

```

### 问题在哪里？

- ❌ **类数量爆炸**
    
- ❌ 每增加一个平台，所有图形都要新增类
    
- ❌ 抽象（图形）和实现（平台）强耦合
    
- ❌ 违反 **开闭原则**
    

---

## 四、桥接模式登场（推荐做法）

### 思路拆解 🧠

把问题拆成 **两个独立变化的维度**：

1. **抽象层（Shape）** —— 图形是什么
    
2. **实现层（Renderer）** —— 怎么画
    

然后用**组合**把它们“桥接”起来。

---

## 五、桥接模式结构
```text
Abstraction（Shape）
   |
   +—— 引用 ——> Implementor（Renderer）
```

---

## 六、C++ 实现示例（标准桥接模式）

### 1️⃣ 实现层：Renderer（平台）

```cpp
#include <iostream>
using namespace std;

class Renderer {
public:
    virtual void drawCircle() = 0;
    virtual ~Renderer() {}
};
```

```cpp
class WindowsRenderer : public Renderer {
public:
    void drawCircle() override {
        cout << "Draw Circle on Windows" << endl;
    }
};

class LinuxRenderer : public Renderer {
public:
    void drawCircle() override {
        cout << "Draw Circle on Linux" << endl;
    }
};
```
---

### 2️⃣ 抽象层：Shape（图形）

```cpp
class Shape {
protected:
    Renderer* renderer;   // 关键：组合而不是继承
public:
    Shape(Renderer* r) : renderer(r) {}
    virtual void draw() = 0;
    virtual ~Shape() {}
};
```

```cpp
class Circle : public Shape {
public:
    Circle(Renderer* r) : Shape(r) {}

    void draw() override {
        renderer->drawCircle();
    }
};
```

---

### 3️⃣ 使用
```cpp
int main() {
    Renderer* win = new WindowsRenderer();
    Shape* circle1 = new Circle(win);
    circle1->draw();

    Renderer* linux = new LinuxRenderer();
    Shape* circle2 = new Circle(linux);
    circle2->draw();

    delete circle1;
    delete circle2;
    delete win;
    delete linux;
}
```

### 输出

```text
Draw Circle on Windows
Draw Circle on Linux
```
---

## 七、C# 版本（同样思想）

```cpp
interface IRenderer {
    void DrawCircle();
}

class WindowsRenderer : IRenderer {
    public void DrawCircle() {
        Console.WriteLine("Draw Circle on Windows");
    }
}

class LinuxRenderer : IRenderer {
    public void DrawCircle() {
        Console.WriteLine("Draw Circle on Linux");
    }
}

```
---

## 八、桥接模式的“妙处”✨

### ✅ 优点总结

|优点|说明|
|---|---|
|解耦|抽象与实现独立变化|
|避免继承爆炸|不再组合出 N × M 个子类|
|符合开闭原则|新平台 / 新图形互不影响|
|更灵活|运行时自由组合|

---

## 九、桥接 vs 适配器（顺带对比）

|模式|目的|
|---|---|
|桥接模式|**设计阶段就分离抽象与实现**|
|适配器模式|**事后兼容已有接口**|

👉 **桥接是主动设计，适配是被动补救**